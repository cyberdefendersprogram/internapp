services:
  app:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data:/var/lib/internapp
    environment:
      - ENV=development
      - SQLITE_PATH=data/app.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
